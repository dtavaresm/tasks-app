import { Container, Typography } from '@mui/material';

function App() {
  return (
    <Container>
      <Typography variant="h1" sx={{ fontWeight: '900', textAlign: 'center', color: 'primary.dark' }}>
        New App
      </Typography>
    </Container>
  )
}

export default App
